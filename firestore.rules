rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Public site reads the data; only signed-in users can edit it.
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
